import Link from "next/link";import {PortalShell,PortalTopbar,StatusBadge} from "../../../components/portal/PortalShell";import {requireUser} from "../../../lib/auth";import {canManageProjects} from "../../../lib/permissions";import {listProjects} from "../../../lib/repositories";import {getPortalLanguage} from "../../../lib/portal-locale";import {portalText} from "../../../data/portal-i18n";
export default async function ProjectsPage(){const user=await requireUser();const language=await getPortalLanguage();const t=(v:string)=>portalText(language,v);const projects=listProjects();return <PortalShell active="/portal/projects"><PortalTopbar eyebrow={t("Project management")} title={t("Projects")} action={canManageProjects(user)?<Link className="os-primary-action" href="/portal/projects/new">+ {t("New project")}</Link>:undefined}/><div className="os-toolbar"><div className="os-search">⌕ <input aria-label={t("Search projects")} placeholder={t("Search projects…")}/></div><div className="os-filter">{t("All statuses")} ▾</div></div><section className="os-card-grid">{projects.map(project=><Link className="os-project-card" href={`/portal/projects/${project.id}`} key={project.id}><div className="os-project-card-top"><span className="os-project-code">PF-{project.id.slice(0,3).toUpperCase()}</span><StatusBadge status={project.status} label={t(project.status)}/></div><h2>{project.name}</h2><p>{project.location}<br/>{project.client}</p><div className="os-project-meta"><div><span>{t("Start")}</span><strong className="os-date-value">{project.startDate||"—"}</strong></div><div><span>{t("Target")}</span><strong className="os-date-value">{project.targetDate||"—"}</strong></div></div><div className="os-progress"><i style={{width:`${project.progress}%`}}/></div><footer><span>{t("Project manager")} · {project.manager}</span><span>{t("Open")} →</span></footer></Link>)}</section></PortalShell>}
