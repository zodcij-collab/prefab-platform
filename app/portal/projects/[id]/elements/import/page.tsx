import {notFound} from "next/navigation";import {PortalShell,PortalTopbar} from "../../../../../../components/portal/PortalShell";import {BackLink} from "../../../../../../components/portal/BackLink";import {ElementImport} from "../../../../../../components/portal/ElementImport";import {requireUser} from "../../../../../../lib/auth";import {canManageProjectElements} from "../../../../../../lib/permissions";import {getProject,listProjectElements} from "../../../../../../lib/repositories";import {getPortalLanguage} from "../../../../../../lib/portal-locale";import {portalText} from "../../../../../../data/portal-i18n";import {importElementsAction} from "../actions";
export default async function ImportPage({params}:{params:Promise<{id:string}>}){const{id}=await params,user=await requireUser(),project=getProject(id);if(!project||!canManageProjectElements(user,id))notFound();const language=await getPortalLanguage(),t=(v:string)=>portalText(language,v);return <PortalShell active="/portal/projects"><BackLink href={`/portal/projects/${id}/elements`} label={t("Back to element register")}/><PortalTopbar eyebrow={project.name} title={t("Import CSV")}/><ElementImport action={importElementsAction} projectId={id} language={language} existingCodes={listProjectElements(id).map((row)=>row.code)}/></PortalShell>}
