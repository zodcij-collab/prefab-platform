import {notFound} from "next/navigation";import {PortalShell,PortalTopbar} from "../../../../../../components/portal/PortalShell";import {ElementForm} from "../../../../../../components/portal/ElementForm";import {requireUser} from "../../../../../../lib/auth";import {canManageProjectElements} from "../../../../../../lib/permissions";import {getProject} from "../../../../../../lib/repositories";import {getPortalLanguage} from "../../../../../../lib/portal-locale";import {portalText} from "../../../../../../data/portal-i18n";import {saveElementAction} from "../actions";
export default async function NewElementPage({params}:{params:Promise<{id:string}>}){const{id}=await params,user=await requireUser(),project=getProject(id);if(!project||!canManageProjectElements(user,id))notFound();const language=await getPortalLanguage(),t=(v:string)=>portalText(language,v);return <PortalShell active="/portal/projects"><PortalTopbar eyebrow={project.name} title={t("Add element")}/><ElementForm action={saveElementAction} projectId={id} language={language} canManage/></PortalShell>}
