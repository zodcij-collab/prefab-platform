import Link from "next/link";
import {PortalShell,PortalTopbar,StatusBadge} from "../../../components/portal/PortalShell";
import {listEmployees,listProjects} from "../../../lib/repositories";
import {getPortalLanguage} from "../../../lib/portal-locale";
import {portalText} from "../../../data/portal-i18n";
import {requireUser} from "../../../lib/auth";
import {canManageEmployees,hasGlobalWorkforceAccess,permittedProjectIds} from "../../../lib/permissions";

export default async function EmployeesPage({searchParams}:{searchParams:Promise<{q?:string;status?:string;project?:string}>}){const user=await requireUser();const language=await getPortalLanguage();const t=(v:string)=>portalText(language,v);if(user.role==="Employee")return <PortalShell active="/portal/employees"><PortalTopbar eyebrow={t("Personnel")} title={t("Restricted")}/><section className="os-panel"><p>{t("Your role does not allow workforce register access.")}</p></section></PortalShell>;const params=await searchParams;const projects=listProjects();const allowed=new Set(permittedProjectIds(user,projects.map((project)=>project.id)));const allowedNames=new Set(projects.filter((project)=>allowed.has(project.id)).map((project)=>project.name));const q=(params.q??"").trim().toLocaleLowerCase();const employees=listEmployees().filter((employee)=>hasGlobalWorkforceAccess(user)||employee.project.split(", ").some((name)=>allowedNames.has(name))).filter((employee)=>!q||`${employee.name} ${employee.role} ${employee.project}`.toLocaleLowerCase().includes(q)).filter((employee)=>!params.status||employee.status===params.status).filter((employee)=>!params.project||employee.defaultProjectId===params.project);return <PortalShell active="/portal/employees"><PortalTopbar eyebrow={`HR · ${t("Personnel")}`} title={t("Employees")} action={canManageEmployees(user)?<Link className="os-primary-action" href="/portal/employees/new">+ {t("Add employee")}</Link>:undefined}/><form className="os-toolbar" method="get"><div className="os-search">⌕ <input name="q" aria-label={t("Search employees")} placeholder={t("Search name, role or project…")} defaultValue={params.q}/></div><select className="os-filter" name="project" defaultValue={params.project??""}><option value="">{t("All projects")}</option>{projects.filter((project)=>allowed.has(project.id)).map((project)=><option value={project.id} key={project.id}>{project.name}</option>)}</select><select className="os-filter" name="status" defaultValue={params.status??""}><option value="">{t("All statuses")}</option>{["Active","Unavailable","Inactive"].map((status)=><option value={status} key={status}>{t(status)}</option>)}</select><button className="os-secondary-action" type="submit">{t("Filter")}</button></form><div className="os-table-wrap os-table-card"><table className="os-table"><thead><tr><th>{t("Employee")}</th><th>{t("Position / trade")}</th><th>{t("Default project")}</th><th>{t("Status")}</th><th>{t("Phone")}</th><th>{t("Manage")}</th></tr></thead><tbody>{employees.map((employee)=><tr key={employee.id}><td><div className="os-person"><span>{employee.name.split(" ").map((part)=>part[0]).join("")}</span><strong>{employee.name}</strong></div></td><td>{t(employee.role)}</td><td>{employee.project}</td><td><StatusBadge status={employee.status} label={t(employee.status)}/></td><td>{employee.phone||"—"}</td><td><Link href={`/portal/employees/${employee.id}`}>{t("View")} →</Link></td></tr>)}</tbody></table>{employees.length===0&&<p className="os-empty-state">{t("No employees match these filters.")}</p>}</div></PortalShell>}
